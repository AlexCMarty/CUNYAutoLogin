---
description: CUNYAutoLogin project context, layout, build, load, dependencies
alwaysApply: true
---

# CUNYAutoLogin — overview

AI agent guidance for this repository.

## What you should do for peak productivity

These are real time savers:

1. Access the internet to search for information when uncertain
2. Run terminal commands
3. Ask the user for clarification

## What this project is

A Manifest V3 browser extension (Firefox + Chromium) that:

1. Stores CUNY login credentials (email, password, TOTP secret) encrypted in `browser.storage.local` using PBKDF2 + AES-GCM.
2. Keeps the vault unlocked across side panel opens for the lifetime of the browser session using `browser.storage.session`.
3. Injects a content script on `https://ssologin.cuny.edu/*` that auto-fills the Oracle SSO login and TOTP pages when the vault session is valid, and also responds to manual `FILL_CREDENTIALS` messages from the dev side panel.
4. Guides a first-time student through a multi-screen onboarding flow (see `.plans/overhaul-onboarding.md` for engineering history).

Recent architecture refactors (merged):
- onboarding stage routing in `onboarding/render.ts` now uses a declarative stage-handler map,
- shared runtime message routing helpers live in `src/runtime/messageRouter.ts`,
- content script responsibilities are split across focused `src/content/*Flow*.ts` and `*Bridge*.ts` modules, with `content.ts` as orchestration only.

Master-password handling, `storage.local` vs `storage.session`, onboarding credential staging, and related security rules are defined only in the **Security, crypto, and gotchas** project rule — do not contradict that rule.

Saved email must end with **`@login.cuny.edu`** (enforced in `sidebar.ts` / `sidebar/sidebar.utils.ts`).

The extension display name in `src/manifest.json` is **CUNYAutoLogin**.

There is no separate transient panel surface in this extension. The vault controller lives in `src/sidebar/vaultController.ts`, rendered inside the sidebar. All user-facing UI lives in `sidebar.html`.

## Project layout

```
sidebar.html                    Vite entry point. Hosts the vault form (<main class="wrap">,
                                sidebar/vaultController.ts) and #onboarding-root (onboarding/render.ts).
src/
  vaultSession/snapshot.ts      `loadVaultSessionSnapshot` — setup / locked / unlocked snapshot
  vaultSession/snapshot.test.ts Unit tests
  sidebar/sidebar.ts            Sidebar entrypoint. Loads onboarding when there is no vault yet,
                                when a session resume snapshot exists, or when the URL hash has
                                `#onboarding=1` (dev/e2e); `#vault=1` forces the vault form on a fresh
                                profile (e2e); otherwise loads the vault UI when a vault exists.
  sidebar/{sidebar.utils,debugPanel,sidebar.css}.ts  Sidebar vault helpers and debug panel modules.
  sidebar/vaultController.ts                Vault controller — setup / locked / unlocked, encrypt/save,
                                session unlock, master rotation, draft autosave. Dev-only debug panel.
  sidebar/sidebar.utils.ts          Pure helpers (email validation, draft parse, status strings,
                                MIN_MASTER_PASSWORD_LENGTH = 12, TOTP source hint helpers)
  sidebar/sidebar.utils.test.ts           Unit tests (jsdom; mocked webextension-polyfill)
  sidebar/debugPanel.ts           Dev-only debug panel (FILL_CREDENTIALS tester + clear-vault button)
  sidebar/sidebar.css               Base vault UI styles
  onboarding/state.ts           18-state enum, bead mapping, resume policy
  onboarding/transitions.ts     Declarative TRANSITION_TABLE + `advance` / `backStateFor`
  onboarding/controller.ts      `createOnboardingController` — closure-only email/password/
                                credentialError state; subscribe/dispatch API
  onboarding/messages.ts        Wire contract + is* guards:
                                  - core: AUTO_FILL_REQUEST, TOTP_SECRET_FROM_PAGE,
                                    STAGE/CLEAR_ONBOARDING_CREDENTIALS (carry secrets)
                                  - onboarding: STAGE_DETECTED, CREDENTIAL_ERROR,
                                    OVERLAY_COMMAND, VERIFY_STATUS, REOPEN_CUNY_TAB,
                                    TAB_REATTACHED, CUNY_TAB_MISSING (metadata-only; default-reject contract)
  onboarding/beadHeader.ts      Five-bead progress header at the top of the sidebar
  onboarding/render.ts          `mountOnboarding` — mounts shell + screen + runtime.onMessage
                                bridge; fires CLEAR_ONBOARDING_CREDENTIALS on unmount.
                                Session-only resume snapshot (`cunyOnboardingResumeSnapshot`;
                                migrates legacy `cunyOnboardingResumeSnapshotV1`) and CUNY-tab
                                close/reopen interruption UX.
  onboarding/screens/           Fully implemented through plan 09:
                                welcome, emailEntry, passwordEntry, openingCuny, allowGate,
                                oaaSpaHome, guidedManage, guidedAddFactor, guidedFactorType,
                                guidedSecretCapture, verifyLoginCode, setDefault, extPasswordSetup.
                                Stubs: biometricOfferStub (plan 10 will replace). screenContext.ts
                                defines the shared mount context.
  onboarding/*.test.ts          Vitest suites (jsdom) alongside each module
  crypto/vault.ts               PBKDF2 + AES-GCM encrypt/decrypt; `VAULT_STORAGE_KEY`, payload types
  crypto/vault.test.ts          Unit tests: round-trips, tamper detection, isStoredVault guard
  cuny/ssoSite.ts               Single source of truth for SSO URL path markers, DOM element IDs,
                                TOTP constants, CUNY_LOGIN_ENTRY_URL, session key names
  cuny/ssoSite.test.ts          Unit tests
  runtime/messageRouter.ts      Shared runtime routing helpers (`routeByType`, `guardedRoute`)
  runtime/messageRouter.test.ts Unit tests
  content/content.ts            IIFE composition root only: startup wiring + URL routing + dev
                                `FILL_CREDENTIALS` listener.
  content/domWait.ts            Shared MutationObserver wait helpers
  content/credentialFlow.ts     Credential submit/error/report flow
  content/totpLoginFlow.ts      Login-page TOTP generation/fill flow
  content/totpEnrollSecretBridge.ts
                                Enroll-page secret scrape/post bridge
  content/mfaEnrollVerifyFlow.ts
                                Self-service enroll-verify polling/retry/pause flow
  content/overlayBridge.ts      Pull/execute overlay commands
  content/allowConsentReporter.ts
                                Allow-button click reporter
  content/banner.ts             Extension-branded credential-error banner on the CUNY tab.
                                Inline styles + INT_MAX z-index; idempotent mount/unmount.
  content/content.utils.ts      Pure helpers (TOTP normalization, enroll scrape, KO-aware input setter, message guards)
  content/content.test.ts       Unit tests (jsdom)
  content/content.bootstrap.test.ts
                                Entrypoint bootstrap regression tests
  content/*Flow*.test.ts        Unit tests for extracted flow modules
  content/*Bridge*.test.ts      Unit tests for extracted bridge modules
  background/service-worker.ts  Opens the side panel on toolbar click (Chromium sidePanel /
                                Firefox sidebarAction). AUTO_FILL_REQUEST prefers the decrypted
                                vault and falls back to the in-memory onboarding staging buffer
                                when the vault isn't set up yet; STAGE/CLEAR_ONBOARDING_CREDENTIALS
                                only ever touch a module-level variable (never storage);
                                TOTP_SECRET_FROM_PAGE → validate + stage in session storage;
                                ONBOARDING_REOPEN_CUNY_TAB opens CUNY_LOGIN_ENTRY_URL (or a
                                provided URL); other onboarding messages are validated and ack-only.
  background/service-worker.test.ts  Unit tests
  manifest.json                 Source manifest; Vite writes dist/manifest.json with
                                side_panel + sidebar_action + action + gecko block
  manifest.e2e.json             E2E variant — adds http://127.0.0.1:4173/* to host_permissions and
                                content_scripts matches for the Playwright fixture server
vite.config.ts                  Builds sidebar + background and emits manifest
vite.content.config.ts          Builds `content.ts` as a single IIFE (`dist/content.js`, inline deps)
e2e/
  onboarding.spec.ts            Playwright: first-run / setup + onboarding screens 1–4 +
                                credential-error regressions.
  onboarding-guided.spec.ts     Overlay, guided CUNY steps, verify / set-default flows.
  onboarding-completion.spec.ts Extension password, biometrics, completion, interruptions, smoke.
  locked.spec.ts                Playwright: vault locked behavior
  unlocked.spec.ts              Playwright: unlocked vault + autofill
  helpers.ts                    Shared helpers: gotoPrimarySurface, clearVaultIfPossible,
                                setupVault, lockVault, walkToPasswordEntry, onboardingHashWith
  extension-fixture.ts          Loads the built extension into Chromium via --load-extension
  fixtures-server.mjs           Local HTTP server for e2e/fixtures/*.html
  fixtures/                     credential (multi-variant), credential-error,
                                credential-success-transient, totp (?next= redirect support),
                                self-service (?view= multi-view SPA simulator),
                                allow-gate (?next= redirect support)
  constants.ts                  Shared constants (FIXTURE_PORT, FIXTURE_ORIGIN, fixture URLs)
  test-credentials.ts           Fabricated test credentials used only by E2E tests
.plans/                         Engineering plan documents (overhaul-onboarding.md, agents/,
                                roles, handoff notes). Not part of the extension build.
.map/                           Versioned, AI-optimized reference for the live CUNY SSO login flow
                                at https://ssologin.cuny.edu/*. Produced by navigating the live site
                                with a real account. Use to write Playwright fixtures, integration
                                tests, and onboarding automation without opening a browser.
                                  pages/         — per-page markdown: selectors, timing, transitions,
                                                   HTML skeleton (credential-entry, totp, allow-gate,
                                                   oaa-spa-home, factors-list, totp-enroll-*)
                                  graph.yaml     — full transition state machine
                                  conventions.md — cross-cutting rules: selector patterns, Oracle JET
                                                   async timing, setInputValue vs keystroke simulation
                                Four Oracle RUI views share one URL (/oaa/rui/index.html?h_ra=1) —
                                differentiate by DOM content only (see .map/README.md).
dist/                           Built extension — load this folder in the browser
```

## Build

```bash
npm install
npm run build          # production: tsc --noEmit → vite build → vite content
npm run build:dev      # development mode on both Vite steps; sidebar includes debug panel
npm run build:e2e      # dev build with manifest.e2e.json (required before E2E tests)
npm run build:content  # rebuild only the content script
npm run watch          # vite build --watch --mode development (sidebar/bg; rerun build:content when content changes)
npm run typecheck      # tsc --noEmit only
npm run test:unit      # vitest run (no build step needed)
npm run test:e2e       # build:e2e then playwright test
```

The two-step Vite build is intentional: `vite.config.ts` bundles the sidebar and background as ES modules, while `vite.content.config.ts` produces a single-file IIFE (`dist/content.js`) with `inlineDynamicImports` — required for reliable MV3 content script injection and to ship `totp-generator` + `neverthrow` inside the content bundle.

## Loading the extension

**Firefox:** `about:debugging` → Load Temporary Add-on → select `dist/manifest.json`

**Chrome/Chromium:** `chrome://extensions` → Developer mode → Load unpacked → select `dist/`

Rebuild and reload the extension after any source change.

## Dependencies (runtime)

- `webextension-polyfill` — unified `browser` API
- `neverthrow` — `Result` / `ResultAsync` / `ok` / `err` in `vault.ts`, `sidebar.ts`, and `content.ts`
- `totp-generator` — TOTP codes in the content script (bundled into IIFE)

## Documentation

README.md should be oriented to less technically inclined college students. It's the first thing they see on Github. Prefer to put technical documentation elsewhere, such as CONTRIBUTING.md.
